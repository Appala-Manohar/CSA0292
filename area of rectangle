#include<stdio.h>
#include<math.h>
int main()
{
int length,width,area;
printf("enter the length:");
scanf("%d",&length);
printf("enter the width:");
scanf("%d",&width);
area=(length*width);
printf("the area=%d",area);
return 0;
}
