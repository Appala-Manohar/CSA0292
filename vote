#include<stdio.h>
int main()
{
    int age=9;
    if (age>=18)
        printf("eligible for vote");
    else 
        printf("not eligible for vote\nu will be eligible for after%d year",18-age);
    return 0;
}
