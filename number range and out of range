#include<stdio.h>
int main()
{
    int num;
    printf("enter the number:\n");
    scanf("%d",&num);
    if (num>=10 && num<=50)
    {
        printf("%d is number within range (10-50)\n");
    }
    else
    {
        printf(" %d the number out of range\n");
    }
    return 0;
}
