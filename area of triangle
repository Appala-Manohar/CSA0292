#include<stdio.h>
#include<math.h>
int main()
{
int base,height,area;
printf("enter the base:");
scanf("%d",&base);
printf("enter the height:");
scanf("%d",&height);
area=(base*height/2);
printf("the area=%d",area);
return 0;
}
